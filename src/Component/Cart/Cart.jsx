import { Helmet } from "react-helmet";

const Cart = () => {
    return (
        <div>
            <Helmet>
            <title>Food Court | Cart</title>
            </Helmet>
            add to card
        </div>
    );
};

export default Cart;