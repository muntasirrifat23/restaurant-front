import { Helmet } from "react-helmet";

const Admin = () => {
  return (
    <div className="hero-content">
      <Helmet>
        <title>Food Court | Admin</title>
      </Helmet>
      <div className="mt-24">Admin</div>
    </div>
  );
};

export default Admin;
